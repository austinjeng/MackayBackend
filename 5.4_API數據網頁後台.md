## 5.4 API數據網頁後台

為使臨床人員能即時掌握病患之復健執行狀況與成效數據，本計畫開發了一套API數據網頁後台系統，作為整體智慧復健評估系統之資料管理與視覺化核心。此後台系統具備雙重角色：其一為RESTful API層，負責接收第5.2節所述行動應用程式上傳之復健數據；其二為伺服器端渲染（Server-Side Rendering, SSR）之網頁儀表板，供臨床人員登入後瀏覽與分析病患之運動紀錄。以下分述各項設計與實作成果。

### 5.4.1 系統架構概述

本後台系統採用Next.js 14框架之App Router架構，以TypeScript作為主要開發語言，部署於Vercel雲端平台。資料庫方面，使用Neon提供之無伺服器PostgreSQL（Serverless PostgreSQL）服務，並透過Prisma ORM進行型別安全之資料庫存取操作。速率限制功能則藉由Upstash Redis實現，採用無伺服器架構以配合整體部署模式。

系統整體分為三層架構：

1. **API服務層（API Layer）**
   - 提供RESTful API端點，接收行動裝置上傳之復健數據
   - 實作身份驗證、資料驗證與速率限制
   - 採用依賴注入（Dependency Injection）之處理函式工廠模式，提升系統可測試性

2. **資料存取層（Data Access Layer）**
   - 透過Prisma ORM操作PostgreSQL資料庫
   - 管理資料模型之關聯、約束與索引
   - 支援交易處理（Transaction）與批次寫入（Bulk Insert）

3. **網頁展示層（Presentation Layer）**
   - 採用伺服器端渲染技術，確保每次載入均取得最新資料
   - 提供逐層深入之導覽介面供臨床人員檢視數據

在隱私保護設計上，由於所有動作辨識之AI推論均於裝置端執行（On-Device Inference），後端僅接收結構化之數值結果（如關節角度、維持秒數、步數等），病患之影像或影片資料全程不經網路傳輸，從根本上避免了視覺隱私資料外洩之風險。

### 5.4.2 資料庫設計

資料庫架構以PostgreSQL為基礎，透過Prisma ORM之Schema定義，將所有資料表與欄位名稱以中文映射，使資料庫結構具備領域語意之可讀性。核心資料模型包含以下五個主要實體：

（一）**病患（Patient）**

儲存病患基本資料，包含病患ID、姓名、出生日期，以及系統自動產生之API金鑰（採用CUID格式），作為行動裝置之身份驗證憑證。每位病患擁有一組唯一之API金鑰。

（二）**運動類型（ExerciseType）**

列舉系統支援之復健運動項目，每項運動包含唯一之程式代碼（code）與中文顯示名稱（name）。系統預設載入五項運動類型：雙膝交替抬高（側）、腳尖對腳跟、左右跨步、深蹲、墊腳尖站立，與第5.1節所述之動作分析標準相對應。

（三）**運動會話（RehabSession）**

代表病患每日之復健期程。透過病患ID與會話日期之唯一約束條件（Unique Constraint），確保每位病患每日僅有一筆會話紀錄。會話狀態以列舉型別表示，包含開啟、關閉與中止三種狀態，並記錄精確之開始與結束時間戳記。

（四）**會話運動（RehabSessionExercise）**

關聯表，連結運動會話與運動類型。透過會話ID與運動類型ID之唯一約束條件，確保同一會話中不會重複記錄相同運動。此實體同樣具備狀態與時間戳記欄位。

（五）**運動紀錄（ExerciseAttempt）**

記錄每次運動嘗試之詳細數據。其結果欄位使用列舉型別（成功、失敗、無效），與第5.1節所定義之動作判定標準一致。數據欄位採用JSONB格式，允許不同運動類型儲存各自所需之量測指標，例如：

- 雙膝交替抬高記錄 `angleDeg`（角度）
- 腳尖對腳跟記錄 `steps`（步數）
- 墊腳尖站立記錄 `angleDeg`（角度）與 `holdSeconds`（維持秒數）
- 左右跨步記錄 `angleDeg`（角度）與 `deviations`（偏移次數）

此彈性之JSON設計使系統無需變更資料庫結構即可支援新的量測欄位，提升系統之擴充性。

各實體間之關聯採用串聯刪除（Cascade Delete）策略，確保刪除病患時其所有相關之會話、運動與紀錄資料一併清除，維持資料一致性。此外，系統於關鍵查詢路徑設置複合索引（Composite Index），如病患ID搭配開始時間之降序索引，以提升資料檢索效能。

### 5.4.3 RESTful API設計

後端系統提供一組RESTful API端點，供行動應用程式進行資料交換。所有端點採用JSON格式進行請求與回應，並遵循一致之錯誤回應格式與HTTP狀態碼規範。主要端點說明如下：

（一）**API金鑰驗證端點**（`GET /api/patients/validate-key`）

行動裝置於啟動時呼叫此端點，驗證其已儲存之API金鑰是否仍然有效。驗證成功後回傳病患之基本資料（姓名、出生日期），供裝置端確認身份。此端點設有額外之速率限制，防止金鑰遭暴力破解。

（二）**復健數據上傳端點**（`POST /api/patients/exercise-attempts`）

此為系統最核心之資料攝取端點，接收行動裝置上傳之運動紀錄。其請求格式包含：

- `exerciseCode`（字串）：運動類型代碼
- `sessionDate`（ISO 8601格式，選填）：會話日期
- `attempts`（陣列）：嘗試紀錄，每筆包含開始時間（`startedAt`）、結束時間（`endedAt`，選填）、結果（`outcome`：success / fail / invalid）、及運動數據（`data`，選填JSON物件）

此端點實作了自動化會話管理機制，處理流程如下：

1. 驗證API金鑰並解析對應之病患身份
2. 驗證運動類型代碼之有效性
3. 檢驗所有時間戳記之格式正確性
4. 以Upsert方式自動建立或取得當日之運動會話（每位病患每日僅一筆）
5. 以Upsert方式建立會話與運動類型之關聯紀錄
6. 透過批次寫入（Bulk Insert）將所有嘗試紀錄一次性存入資料庫

此設計使行動端無需自行管理會話之生命週期，大幅簡化了客戶端之實作複雜度。上傳成功後回傳會話ID、會話運動ID及寫入筆數，供裝置端確認。

（三）**管理者病患列表端點**（`GET /api/admin/patients`）

供網頁儀表板呼叫，回傳所有已註冊病患之資料及其復健會話紀錄。此端點受管理者身份驗證保護，僅具備管理者角色之使用者方可存取。

（四）**系統健康檢查端點**（`GET /api/health`）

公開端點，透過執行資料庫查詢確認系統運作狀態，回傳服務狀態與延遲時間，供部署監控與可用性檢測使用。

### 5.4.4 管理者儀表板介面

網頁儀表板採用伺服器端渲染技術，所有頁面均設定為動態渲染（Force Dynamic），確保臨床人員每次存取均取得最新之病患復健資料。介面設計採用逐層深入（Drill-Down）之導覽模式，由病患列表層層深入至個別運動之詳細紀錄。

（一）**病患列表首頁**

登入後之主畫面，以列表形式展示所有已註冊之病患，顯示病患姓名及由出生日期動態計算之年齡，供臨床人員快速瀏覽與選擇欲檢視之病患。

（二）**復健歷史頁面**

選擇特定病患後進入此頁面，系統將該病患之所有運動會話依日期分組排列，採用台灣時區（Asia/Taipei）之中文格式顯示日期與時間。會話依時間倒序排列，使最近之復健紀錄優先呈現。

（三）**會話詳細頁面**

展示特定會話中所執行之各項運動，包含運動類型名稱（中文）、開始時間及嘗試紀錄筆數。系統於此頁面實施所有權驗證，確認該會話確屬URL中指定之病患，防止透過URL操作存取他人資料。

（四）**運動詳細頁面**

此為最核心之資料分析介面，包含以下兩個主要區塊：

- **統計面板**：以色彩區分之數值卡片呈現成功次數（綠色）、失敗次數（紅色）及計算所得之成功率（百分比），使臨床人員能直覺地掌握病患於該項運動之整體表現。

- **動態資料表格**：系統自動從各筆紀錄之JSON數據欄位中擷取所有鍵值，動態產生對應之表格欄位。欄位標題透過本地化映射字典將英文鍵名轉換為中文顯示（如 `angleDeg` 顯示為「角度」、`holdSeconds` 顯示為「維持秒數」、`steps` 顯示為「步數」、`deviations` 顯示為「偏移次數」）。每筆紀錄之運動結果亦以顏色編碼呈現（綠色代表成功、紅色代表失敗、灰色代表無效），使臨床人員能快速識別病患之動作品質趨勢。

### 5.4.5 安全機制與存取控制

系統採用雙重身份驗證架構，針對兩類不同使用者提供各自適配之認證機制：

（一）**病患端API金鑰驗證**

每位病患於建立時，系統自動產生一組CUID格式之API金鑰，具備唯一性。行動裝置透過HTTP請求標頭（`X-API-Key`）攜帶此金鑰進行身份識別。驗證採用二階段設計：中介層（Middleware）於Edge Runtime中首先檢查標頭是否存在，缺失則立即回傳401未授權回應；隨後API處理函式於Node.js Runtime中將金鑰與資料庫進行比對，解析出對應之病患身份。此二階段設計兼顧了Edge Runtime之效能限制與資料庫查詢之完整驗證需求。

（二）**管理者網頁會話驗證**

臨床人員透過帳號密碼登入，密碼採用bcrypt雜湊演算法儲存。驗證成功後由NextAuth.js簽發JWT令牌，存放於HTTP-only Cookie中。中介層對所有病患相關頁面強制要求有效之網頁會話，未認證之請求將自動導向登入頁面。

（三）**速率限制**

採用Upstash Redis實作滑動窗口（Sliding Window）演算法，以API金鑰或用戶端IP位址作為識別依據，限制請求頻率，有效防止惡意之暴力破解或服務濫用攻擊。

（四）**跨病患存取防護**

系統於API處理函式層級實施嚴格之存取控制：病患僅能查詢自身之資料（API金鑰所對應之病患ID須與URL參數一致）；網頁儀表板之各層頁面均驗證資料之所有權鏈（如會話須屬於指定病患、運動須屬於指定會話），防止透過URL參數竄改存取他人之復健紀錄。

所有資料傳輸均透過HTTPS加密，確保敏感資訊（API金鑰、病患資料）於傳輸過程中之安全性。

### 5.4.6 系統部署與持續整合

系統部署於Vercel雲端平台，透過與Git版本庫之整合實現持續整合與持續部署（CI/CD），每次推送程式碼至主分支即自動觸發建置與部署流程，確保系統版本之即時更新。資料庫託管於Neon無伺服器PostgreSQL服務，具備自動擴展與連線池管理功能，適應醫療應用場景下之變動式使用量需求。

資料庫結構之版本控制透過Prisma Migrate管理，正式環境之初始化資料（運動類型與管理者帳號）採用Upsert操作寫入，避免影響既有之病患與復健資料。系統組態透過環境變數集中管理，包含資料庫連線字串、認證金鑰、Redis服務位址等，遵循現代雲原生應用之最佳實踐。

完整之資料流程如下：臨床人員於系統建立病患資料後，系統自動產生API金鑰；將金鑰配置至病患之行動裝置後，裝置啟動時即呼叫驗證端點確認金鑰有效性；病患進行復健運動時，裝置端完成AI動作推論後，將結構化之運動結果透過API上傳至後端；後端自動管理每日會話並將資料寫入資料庫；最終，臨床人員登入網頁儀表板，即可逐層瀏覽病患之復健進度與成效統計，達成本計畫「提供醫護人員患者復健情形資訊」之目標。
